name: IaC Security Scan Guardrail & OPA Gate

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'terraform/**'
      - 'policy/**'
      - '**.tf'
  push:
    branches:
      - main
    paths:
      - 'terraform/**'
      - 'policy/**'
      - '**.tf'

permissions:
  contents: read
  pull-requests: write

jobs:
  checkov-guardrail:
    name: Checkov Static Analysis Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Checkov Engine
        run: |
          python -m pip install --upgrade pip
          pip install checkov

      - name: Execute Checkov IaC Scan (Hard Gate)
        id: checkov_scan
        run: |
          checkov -d ./terraform/ \
            --framework terraform \
            --compact \
            --check HIGH,CRITICAL \
            --output cli

  opa-conftest-gate:
    name: OPA / Conftest Policy Enforcement Gate
    runs-on: ubuntu-latest
    needs: checkov-guardrail
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.6.0'

      - name: Install Conftest CLI
        run: |
          CONFTEST_VERSION="0.50.0"
          curl -LO "https://github.com/open-policy-agent/conftest/releases/download/v${CONFTEST_VERSION}/conftest_${CONFTEST_VERSION}_Linux_x86_64.tar.gz"
          tar xzf "conftest_${CONFTEST_VERSION}_Linux_x86_64.tar.gz"
          sudo mv conftest /usr/local/bin/
          conftest --version

      - name: Initialize & Generate Terraform Plan JSON
        working-directory: ./terraform
        env:
          AWS_DEFAULT_REGION: us-east-1
          AWS_ACCESS_KEY_ID: mock_key
          AWS_SECRET_ACCESS_KEY: mock_secret
        run: |
          terraform init -backend=false
          terraform plan -out=tfplan.binary
          terraform show -json tfplan.binary > tfplan.json

      - name: Execute OPA Rego Evaluation
        run: |
          conftest test ./terraform/tfplan.json \
            --policy ./policy \
            --namespace terraform.security
